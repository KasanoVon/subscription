# subscription

## Run

1. Install dependencies:
   `npm install`
2. Start auth API (SQLite):
   `npm run server`
3. In another terminal, start frontend:
   `npm run dev`

The auth database file is created at `server/subnote.db`.
